import bcrypt from "bcrypt";

class HashingLibrary {
  static async hashPassword(password) {}
  static async matchPassword(passwordRequest, passwordDatabase) {}
}

export default new HashingLibrary();
